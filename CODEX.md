# Codex Adapter

Use `AGENTS.md` for repository-wide RateEngine governance and any nested `AGENTS.md` for the files being changed.

At task start, apply the routing table in `AGENTS.md` Section 6 and load every matching procedure under `.codex/skills/`. Route by credible impact, not just edited directory. Skills may compose; reuse evidence rather than skipping a matching skill.

When the user asks to finish a job end-to-end, carry it through the repository definition of done: prove intended and adjacent behavior, verify the final head, clear blockers/review findings, and merge when authorized. Do not stop at code-written, PR-opened, or partial-CI states.

Preserve unrelated working-tree changes and report exact validation results in the final handoff.
