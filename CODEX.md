# Codex Adapter

Use `AGENTS.md` for repository-wide RateEngine governance and any nested `AGENTS.md` for the files being changed.

Codex-specific procedures are discoverable under `.codex/skills/`. Load them only when their trigger matches the task. Preserve unrelated working-tree changes and report exact validation results in the final handoff.
