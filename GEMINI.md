# Gemini CLI Adapter

Use `AGENTS.md` for repository-wide RateEngine governance and any nested `AGENTS.md` for the files being changed.

Use the task-routing table in `AGENTS.md` Section 6 as the shared verification authority. When a listed `.codex/skills/*/SKILL.md` procedure matches the task, follow its verification contract as repository guidance even when working through Gemini; do not create a separate Gemini copy of the same procedure.

Use any separately configured Gemini-specific workflow only when it is available and applicable. Verify architecture and deployment facts against current source and canonical documentation rather than duplicating them here. Do not treat a Gemini automation failure by itself as an application regression when the authoritative repository CI evidence says otherwise.
