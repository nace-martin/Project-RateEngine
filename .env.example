## Example environment for Project-RateEngine (backend)

# PostgreSQL connection string (required by Django settings)
# Format: postgres://USER:PASSWORD@HOST:PORT/DBNAME
DATABASE_URL=postgres://postgres:postgres@localhost:5432/rateengine

# Optional: Django settings
#DEBUG=True

# SECRET_KEY: Always set in production. If omitted locally, the app uses a
# dev-only insecure default and will emit a warning at startup. Use a strong
# random value (e.g., `python -c "import secrets; print(secrets.token_urlsafe(50))"`).
#SECRET_KEY=replace-with-a-dev-secret

# Hosts allowed to serve the app (comma-separated)
#ALLOWED_HOSTS=127.0.0.1,localhost

# Optional: CORS origins (comma-separated)
#CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
